"use strict";
// db-utils.ts
Object.defineProperty(exports, "__esModule", { value: true });
/*
const config = {
    user: 'your_user',
    password: 'your_password',
    server: 'your_server_name', // e.g., 'localhost\\SQLEXPRESS' or '<servername>.database.windows.net'
    database: 'your_database',
    options: {
        encrypt: true, // Use this for Azure SQL
        trustServerCertificate: true // Use this for local dev/self-signed certs
    }
};

export async function runQuery(query: string) {
    try {
        await sql.connect(config);
        const result = await sql.query(query);
        return result.recordset;
    } catch (err) {
        console.error('Database error:', err);
    } finally {
        await sql.close();
    }
 
}
*/ 
//# sourceMappingURL=db.util.js.map